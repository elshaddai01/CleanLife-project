# Expo Go discovers the development computer automatically; only the port is needed.
EXPO_PUBLIC_API_PORT=3001

# `npm start` generates .env.local with the active computer IP automatically.

# Set an explicit public URL only for standalone/production builds.
# EXPO_PUBLIC_API_BASE_URL=https://api.example.com
